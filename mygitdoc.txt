# Get files
git clone https://github.com/stefanleipoldseal/git

# Check files
git status
git add .
git commit	# Git comment cannot start with #

# Push files
git push

if not working
git push -u origin master
